// types
import { ListItemIconProps } from 'shared/core/dataDisplay/ListItemIcon/ListItemIcon.types';

// styles
import { StyledListItemIcon } from 'shared/core/dataDisplay/ListItemIcon/ListItemIcon.styles';

export const ListItemIcon = (props: ListItemIconProps) => {
	return <StyledListItemIcon {...props} />;
};
