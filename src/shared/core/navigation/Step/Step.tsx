// types
import { StepProps } from 'shared/core/navigation/Step/Step.types';

// styles
import { StyledStep } from 'shared/core/navigation/Step/Step.styles';

export const Step = (props: StepProps) => {
	return <StyledStep {...props} />;
};
